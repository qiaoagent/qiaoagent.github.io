# bio/

Master copies of Qiao Jin's bios. `homepage.md` is rendered by index.html at load; edit it and the page changes. Each `.md` has a plain-text `.txt` twin with the links removed, made by `python3 bio/build.py`; run it after every edit. Add other variants (a talk bio, a one-liner) as new `.md` files here.
